import { cn } from "@/lib/utils";
import { useCollapse } from "@/store/useCollapse";

const RecommendChannel = () => {
  return (
    <div className={cn("text-sm font-semibold")}>RECOMMENDED CHANNELS</div>
  );
};

export default RecommendChannel;
